/**
 * @file
 * ad_theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.adTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
